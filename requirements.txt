# Mirror of [project.dependencies] in pyproject.toml. Kept in sync manually
# because Railpack's Python provider only auto-installs from requirements.txt
# (it does not parse hatchling-style PEP 621 dependencies).
anthropic>=0.40.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
rank-bm25>=0.2.2
pyyaml>=6.0.2
python-multipart>=0.0.12
httpx>=0.27.0
tenacity>=9.0.0
